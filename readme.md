# Setup and Build Guide

This repository contains both a Python implementation (master branch) and a C++ Qt implementation (qt branch). The provided script automates the environment setup for both.

## Prerequisites
* **Windows 10/11**
* **PowerShell** (Run as Administrator)
* **Git** installed and configured

## How to Run the Script

1. **Open PowerShell as Administrator** (Right-click Start > Terminal (Admin) or PowerShell (Admin)).
2. **Navigate to the project folder**:
   ```powershell
   cd path/to/sublinear
